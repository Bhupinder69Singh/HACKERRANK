(SELECT CITY,
CHAR_LENGTH(CITY) AS city_length
FROM STATION
ORDER BY city_length ASC,CITY ASC
LIMIT 1)
UNION(SELECT CITY,
CHAR_LENGTH(CITY) AS city_length
FROM STATION
ORDER BY city_length DESC,CITY DESC
LIMIT 1)
